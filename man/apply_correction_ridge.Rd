% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ridge_fit_apply.R
\name{apply_correction_ridge}
\alias{apply_correction_ridge}
\title{Apply ridge correction to data}
\usage{
apply_correction_ridge(fit, Xlog, batch, ref_batch = NULL)
}
\arguments{
\item{fit}{object of class "anchor_ridge_fit" from fit_anchor_ridge}

\item{Xlog}{numeric matrix (genes x samples) in log scale}

\item{batch}{factor of batch labels}

\item{ref_batch}{optional reference batch label to keep uncorrected}
}
\value{
corrected matrix in log scale
}
\description{
Subtracts estimated batch effects from samples. Data are standardized before
applying corrections (using stored standardization parameters), then unstandardized
after correction. If ref_batch is specified, effects are recentered so the
reference batch has zero correction. Otherwise, global means are preserved.
}
\examples{
\dontrun{
# After fitting
Xc <- apply_correction_ridge(fit, Xlog, batch)

# With reference batch
Xc_ref <- apply_correction_ridge(fit, Xlog, batch, ref_batch = "A")
}
}
