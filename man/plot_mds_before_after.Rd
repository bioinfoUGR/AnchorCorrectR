% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/mds_utils.R
\name{plot_mds_before_after}
\alias{plot_mds_before_after}
\title{Plot MDS before vs after, side by side, colored by batch (optionally shaped by biology)}
\usage{
plot_mds_before_after(
  x_before,
  x_after,
  batch,
  biology = NULL,
  input_type = c("auto", "counts", "log"),
  distance = c("euclidean", "1-corr"),
  k = 2,
  same_limits = TRUE,
  title = "MDS before vs after"
)
}
\arguments{
\item{x_before}{Matrix (genes x samples) before correction.}

\item{x_after}{Matrix (genes x samples) after correction.}

\item{batch}{Factor/character of length \code{ncol(x_before)} used for point color.}

\item{biology}{Optional factor/character for point shape.}

\item{input_type}{One of \code{c("auto","counts","log")}.}

\item{distance}{One of \code{c("euclidean","1-corr")}.}

\item{k}{Integer, MDS dimensions to compute (2 recommended for plotting).}

\item{same_limits}{Logical; enforce the same x/y limits across both panels (default TRUE).}

\item{title}{Overall plot title (default \code{"MDS before vs after"}).}
}
\value{
A ggplot object (if ggplot2 is installed) or \code{NULL} (after drawing base plots).
}
\description{
Produces two panels (before/after) with identical axis limits so the layouts are comparable.
Styling matches the QC utility plots (\code{theme_bw}, point size/alpha).
If ggplot2 is available, returns a ggplot object with facet panels; otherwise, draws base R plots.
}
