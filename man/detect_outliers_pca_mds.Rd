% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/outliers_detection.R
\name{detect_outliers_pca_mds}
\alias{detect_outliers_pca_mds}
\title{Detect Outliers in Gene Expression Data using PCA and MDS}
\usage{
detect_outliers_pca_mds(
  data_matrix,
  n_sd = 5,
  min_gene_count = 10,
  min_gene_samples = 5,
  min_total_count = 0,
  max_iterations = 20,
  verbose = TRUE
)
}
\arguments{
\item{data_matrix}{Numeric matrix of expression values (genes in rows, samples in columns).
Row names should be gene IDs, column names sample IDs.}

\item{n_sd}{Numeric. Number of standard deviations for outlier threshold (default 5).}

\item{min_gene_count}{Numeric. Minimum count value for gene filtering (default 10).}

\item{min_gene_samples}{Integer. Minimum samples with min_gene_count for gene inclusion (default 5).}

\item{min_total_count}{Numeric. Minimum total count per gene for inclusion (default 0).}

\item{max_iterations}{Integer. Maximum outlier removal iterations (default 20).}

\item{verbose}{Logical. Print iteration progress (default TRUE).}
}
\value{
List containing:
\item{pc_plot}{ggplot object for PCA results}
\item{mds_plot}{ggplot object for MDS results}
\item{filter_table}{data.frame with sample IDs and outlier status}
\item{outliers}{character vector of outlier sample IDs}
\item{n_iterations}{number of iterations performed}
}
\description{
Iteratively identifies outlier samples that deviate beyond a threshold
in either PCA or MDS space. Stops when no new outliers are detected.
}
\examples{
\dontrun{
result <- detect_outliers_pca_mds(
  data_matrix = counts,
  n_sd = 4,
  verbose = TRUE
)
print(result$pc_plot)
table(result$filter_table$filter_outliers)
}
}
