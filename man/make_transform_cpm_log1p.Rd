% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/make_transform_cpm_log1p.R
\name{make_transform_cpm_log1p}
\alias{make_transform_cpm_log1p}
\title{Create forward/inverse transforms for count or log data}
\usage{
make_transform_cpm_log1p(
  x,
  input_type = c("auto", "counts", "log"),
  scale_factor = 1e+06,
  pseudocount = 0
)
}
\arguments{
\item{x}{numeric matrix (genes x samples)}

\item{input_type}{"auto", "counts", or "log"}

\item{scale_factor}{CPM scaling factor (default 1e6)}

\item{pseudocount}{additional pseudocount before log (default 0)}
}
\value{
list with:
\itemize{
\item input_type: detected or specified type
\item forward: function to transform to log scale
\item inverse_counts_from_log: function to transform back to count-like values
}
}
\description{
This function creates transformation functions for converting between
count and log-scale data. For counts, it applies CPM normalization
followed by log1p transformation. For log data, it acts as identity.
}
\examples{
\dontrun{
# For count data
counts <- matrix(rpois(1000, 50), nrow = 100)
tf <- make_transform_cpm_log1p(counts, input_type = "counts")
log_data <- tf$forward(counts)
counts_back <- tf$inverse_counts_from_log(log_data)

# For log data
log_data <- matrix(rnorm(1000, 5, 2), nrow = 100)
tf <- make_transform_cpm_log1p(log_data, input_type = "log")
log_out <- tf$forward(log_data)  # Identity transform
}
}
\keyword{internal}
