% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/replicates_fastq_bootstrap.R
\name{generate_bootstrap_fastq}
\alias{generate_bootstrap_fastq}
\title{Generate bootstrap replicates of FASTQ files}
\usage{
generate_bootstrap_fastq(
  input_dir,
  file_names,
  output_dir,
  reps = 3,
  percent = 0.5,
  merge_inputs = TRUE,
  merge_label = "merged",
  use_parallel = TRUE,
  workers = NULL,
  chunk_size = 1e+05,
  seed = NULL
)
}
\arguments{
\item{input_dir}{Directory containing input FASTQ or FASTQ.gz files.}

\item{file_names}{Character vector of sample prefixes. For each prefix the
function searches \code{input_dir} for files named \code{prefix.fastq[.gz]} (single
end) or \code{prefix_1.fastq[.gz]}/\code{prefix_2.fastq[.gz]} (paired-end). Presence
of both \verb{_1} and \verb{_2} files triggers paired processing automatically.}

\item{output_dir}{Directory where bootstrap FASTQ files will be written. Will
be created recursively if it does not exist.}

\item{reps}{Integer; number of bootstrap replicates to generate per file.}

\item{percent}{Numeric in (0, 1]; proportion of reads sampled (with
replacement) for each bootstrap replicate.}

\item{merge_inputs}{Logical; when \code{TRUE} (default) reads from all inputs are
merged prior to bootstrapping and one merged replicate (per requested
\code{reps}) is written out. Set to \code{FALSE} to generate replicates separately for
each input file or pair.}

\item{merge_label}{Character scalar used as the base file name when
\code{merge_inputs = TRUE}.}

\item{use_parallel}{Logical; when \code{TRUE} (default) bootstrap replicates are
generated in parallel via \code{parallel::mclapply()}. Set to \code{FALSE} to force
sequential execution.}

\item{workers}{Integer; number of parallel workers to use when
\code{use_parallel = TRUE}. Defaults to \code{parallel::detectCores() - 1}.}

\item{chunk_size}{Integer; number of reads pulled per chunk when streaming
FASTQ data. Larger values reduce disk I/O but increase peak memory while
generating each replicate.}

\item{seed}{Optional integer to set a reproducible RNG seed.}
}
\value{
Invisibly returns \code{TRUE} if all replicates are generated.
}
\description{
Randomly resamples reads with replacement from specified FASTQ/FASTQ.gz files
and writes bootstrap replicates to \code{output_dir}.
}
\examples{
\dontrun{
generate_bootstrap_fastq(
  input_dir = "fastq_inputs",
  file_names = c("sample1", "sample2"),
  output_dir = "replicates_fastq_bootstrap",
  reps = 5,
  percent = 0.25,
  seed = 123
)
}
}
