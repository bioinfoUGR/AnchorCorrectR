% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/mds_utils.R
\name{mds_samples}
\alias{mds_samples}
\title{Compute sample-level MDS on log-scale data}
\usage{
mds_samples(
  x,
  input_type = c("auto", "counts", "log"),
  distance = c("euclidean", "1-corr"),
  k = 2
)
}
\arguments{
\item{x}{Matrix (genes x samples), counts or log-scale.}

\item{input_type}{One of \code{c("auto","counts","log")}. If "auto",
the transform is chosen based on the range of values.}

\item{distance}{One of \code{c("euclidean","1-corr")}. If "1-corr", uses
\code{1 - cor(X)} as a distance on samples.}

\item{k}{Integer, number of MDS dimensions to return (default 2).}
}
\value{
A \code{data.frame} with columns \code{sample}, \code{Dim1}, \code{Dim2}, ... up to \code{Dim k}.
}
\description{
Transforms input to log scale (CPM + log1p if counts), computes a sample-sample
distance (Euclidean or 1 - Pearson correlation), then runs classical MDS
(\code{cmdscale}) to obtain low-dimensional coordinates.
}
